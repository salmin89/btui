export enum BTCommand {
  listConnected = 'list:connected',
  listPaired = 'list:paired',
}

export enum DeviceCommand {
  connect = 'connect',
  disconnect = 'disconnect',
  reconnect = 'reconnect',
  unpair = 'unpair',
  info = 'info',
  return = 'return',
}